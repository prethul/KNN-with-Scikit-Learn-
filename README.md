# KNN-with-Scikit-Learn-
A learning repository focused on the K-Nearest Neighbors (KNN) algorithm. It includes explanations of how KNN works, along with practical experiments using cross-validation, parameter tuning, and grid search techniques.
